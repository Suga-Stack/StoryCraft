# StoryCraft 前端环境变量配置示例

# API 基础 URL
# 开发环境使用本地后端地址
VITE_API_BASE_URL=https://storycraft.work.gd

# WebSocket 基础 URL
# 开发环境使用本地 WebSocket 地址
VITE_WS_BASE_URL=wss://storycraft.work.gd

# 是否使用 Mock 数据
# true: 使用前端 Mock 数据,不依赖后端
# false: 使用真实后端 API
VITE_USE_MOCK=false

# 应用标题
VITE_APP_TITLE=StoryCraft

# 是否启用调试模式
VITE_DEBUG=false

# API 请求超时时间(毫秒)
VITE_API_TIMEOUT=30000

# 上传文件大小限制(MB)
VITE_MAX_UPLOAD_SIZE=10
